apiVersion: v1
kind: Secret
metadata:
  name: microfinance-secrets
  namespace: microfinance
type: Opaque
stringData:
  DB_PASSWORD: "CHANGE_ME_BEFORE_DEPLOY"
  REDIS_PASSWORD: "CHANGE_ME_BEFORE_DEPLOY"
  MONGO_PASSWORD: "CHANGE_ME_BEFORE_DEPLOY"
  JWT_SECRET: "CHANGE_ME_BEFORE_DEPLOY_MIN_32_CHARS"
  MINIO_ACCESS_KEY: "CHANGE_ME_BEFORE_DEPLOY"
  MINIO_SECRET_KEY: "CHANGE_ME_BEFORE_DEPLOY"
  SMTP_PASSWORD: "CHANGE_ME_BEFORE_DEPLOY"
  AFRICAS_TALKING_KEY: "CHANGE_ME_BEFORE_DEPLOY"
# IMPORTANT: Replace all values above before deploying to production.
# Recommended: Use External Secrets Operator with AWS Secrets Manager or HashiCorp Vault.
