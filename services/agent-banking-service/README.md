# agent-banking-service

Minimal skeleton for the Agent Banking Service.

Run locally:

```bash
python -m venv .venv
source .venv/bin/activate  # or .venv\\Scripts\\activate on Windows
pip install -r requirements.txt
python src/app.py
```